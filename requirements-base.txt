pdfplumber>=0.11
openpyxl>=3.1
pymupdf>=1.24

# OCR - lê os NOMES das rubricas nas fichas cifradas do Estado quando o código
# ainda não está mapeado. opencv-python-headless vem antes de propósito, pra o
# rapidocr não puxar o opencv-python "cheio" (que precisa de libGL no servidor).
opencv-python-headless>=4.9
rapidocr-onnxruntime>=1.3
