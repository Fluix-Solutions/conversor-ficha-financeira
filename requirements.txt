# Deploy web (Railway/Render). Instala tudo: engine + servidor + OCR.
# Sem "-r requirements-base.txt": o Railpack copia só este arquivo antes do
# pip install, então um include quebra o build. Manter em sincronia com
# requirements-base.txt.
pdfplumber>=0.11
openpyxl>=3.1
pymupdf>=1.24
flask>=3.0
gunicorn>=21.0

# OCR - necessário para converter fichas ESCANEADAS (PDF de imagem, sem
# texto), Serra e Estado. Pesa ~210 MB (opencv sozinho são ~118 MB) e o pico
# de RAM de uma conversão chega a ~580 MB: a instância precisa de folga.
# opencv-python-headless vem antes de propósito, pra o rapidocr não puxar o
# opencv-python "cheio".
opencv-python-headless>=4.9
rapidocr-onnxruntime>=1.3
